<!-- start snippets/cart-upsell - -->

{% assign cart_items = cart.items %}

{% assign upsell_collection = settings.upsell_collection %}
{% assign upsell_products = collections[upsell_collection].products %}
{% assign limit_products = settings.products_cart_page_upsell | default: 4 %}

{% assign class_hide = '' %}
{% if cart.items.size > 0 %}
  {% assign class_hide = 'hide' %}
{% endif %}

<style>
  .cart__upsel-wrapper {
    margin-bottom: 10px;
  }
  .cart__upsell-title {
    display: block;
    padding: 21px 0 31px;
    text-align: center;
    width: 100%;
    font-size: 37px;
    letter-spacing: -0.2px;
    color: #000;
  }

  .cart__upsell-slider {
    display: flex;
    width: 90%;
    max-width: 1167.7px;
    margin: 0 auto;
    position: relative;
    padding-bottom: 35px;
  }

  .cart__upsell-slider:not(.slick-slider) {
    width: 100%;
    justify-content: center;
    flex-wrap: wrap;
    gap: 25px;
    padding: 0 20px 35px;
  } 

  .cart__upsell-slider .slick-track {
    display: flex;
    align-items: stretch;
  }

  .cart__upsell-slider .slick-list {
    overflow: hidden;
  }

  .cart__upsell-product-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .cart__upsell-atc-form {
    padding: 0 14px;
    margin: 8px 0 9px 0;
    display: flex;    
    align-items: flex-start;
    justify-content: space-between;
    flex-direction: column;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-form {
    margin-top: 7px;
    padding: 0 14px;
  }

  .cart__upsell-slider .slick-slide {
    width: 328px;
    border: 1px solid #e8e8e8;
    margin: 0 6.1px;
    height: fit-content;
  }

  .cart_upsell-list-items.cart__upsell-slider > .cart__upsell-product {
    width: 290px;
    border: 1px solid #e8e8e8;
    height: fit-content;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) {
    width: 326px;
    transform: scale(0.7822);
    translate: 0 2px;
  }

  .cart__upsell-slider:not(.slick-slider) .cart__upsell-product-image-wrapper {
    padding-top: 110%;
    position: absolute;
    width: 100%;
    position: relative;
    overflow: hidden;
  }

  .cart__upsell-slider:not(.slick-slider) .cart__upsell-product-image-wrapper .cart__upsell-product-image {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
  }

  .cart__upsell-slider.slick-slider .cart__upsell-product-image-wrapper {
    overflow: hidden;
    height: 351px;
  }

  .cart__upsell-slider.slick-slider .slick-slide:not(.slick-center) .cart__upsell-product-image-wrapper {
    height: 349px;
  }

  .cart__upsell-atc-wrapper {
    padding: 0 1px;
  }

  .cart__upsell-slider .slick-arrow {
    width: 50px;
    height: 50px;
    position: absolute;
    border-radius: 50%;
    top: 206px;
    z-index: 2;
    background-color: #ffffffa8;
  }

  .cart__upsell-slider .prev-arrow {
    left: 85px;
  }

  .cart__upsell-slider .next-arrow {
    right: 85px;
    transform: rotate(180deg);
  }

  .cart__upsell-slider .slick-arrow:before {    
    content: "";
    width: 100%;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;
    background-size: 50%;
    background-position: center center;
    background-repeat: no-repeat;
    background-image: url("{{ 'arrow-right.svg' | asset_url }}");
  }

  .cart__upsell-product-title {
    font-size: 20px;
    letter-spacing: 0.7px;
    margin-top: 22px;
    line-height: 1.2;
    padding: 0 14px;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-title {
    margin-top: 21px;
    padding: 0 13.3px;
    letter-spacing: 0.65px;
  }

  .cart__upsell-product-title * {
    color: #252525;
    font-weight: 700;
  }

  .cart__upsell-product-review {
    padding: 0 12px;
    margin-top: 11.1px;
    display: flex;
    align-items: center;
    gap: 13px;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-review {
    padding: 0 12px;
    margin-top: 10.9px;
    gap: 12px;
  }
  
  .cart__upsell-product-review svg {
    width: 68px;
  }

  .cart__upsell-product-review svg path {
    fill: {{ settings.upsell_review_stars_color }};
  }

  .cart__upsell-product-review span {
    font-size: 10px;
    text-transform: capitalize;
    letter-spacing: -0.03px;  
  }

  .cart__upsell-form-fields-wrapper {
    display: flex;
    justify-content: space-between;
    width: 100%;
  }

  .cart__upsell-select-wrapper {
    width: 50%;
    gap: 32px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }

  .cart__upsell-product-select {
    color: #161616;
    width: 100%;
    height: 24px;
    font-size: 14px;
    padding: 0 21px 2px 8px;
    border: 1px solid #000;
    appearance: none;
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    background: url('{{ "vector.svg" | asset_url }}') no-repeat 91% 50%;
    background-size: 11px;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-select {
    padding: 0 22px 0 6px;
    background-size: 12px;
    background-position-x: 89%;
    letter-spacing: -0.5px;
    font-size: 15px;
  }

  .cart__upsell-product-atc-wrapper {
    width: 100%;
    display: flex;
    align-items: flex-end;
    justify-content: flex-end;
    flex-direction: column;
    gap: 11px;
    margin-top: 14px;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-atc-wrapper {
    gap: 12px;
  }

  .cart__upsell-product-price {
    font-size: 20px;
    font-weight: 700;
    color: {{ settings.upsell_price_color }};
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-price {
    letter-spacing: -0.1px;
  }

  .cart__upsell-atc-btn-wrapper {
    position: relative;
    width: 100%;
  }

  .cart__upsell-atc-price {
    width: 100%;
    height: 34px;
    background: {{ settings.upsell_atc_btn_background }};
    border: 1px solid {{ settings.upsell_atc_btn_border_color }};
    color: {{ settings.upsell_atc_btn_text_color }};
    font-size: 16px;
    border-radius: 5px;
    position: relative;
  }

  .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-price {
    height: 34px;
    letter-spacing: -0.4px;
    padding-top: 1px;
  }

  .cart__upsell-atc-price[disabled] {
    background-color: #eee;
    color: #bbb;
    border: 1px solid #bbb;
  }

  .upsell-produce-atc___mask {
    display: none;
    position: absolute;
    top: 0px;
    right: 0px;
    bottom: 0px;
    left: 0px;
  }

  .cart__upsell-atc-price[disabled] + .upsell-produce-atc___mask {
    display: block;
  }

  .cart__upsell-alert {
    display: none;
    padding: 0 14px;
    color: red;
    font-size: 14px;
  }

  @media (max-width: 1019px) {
    .cart__upsel-wrapper {
      padding-top: 6px;
    }

    .cart__upsell {
      padding: 0;
    }

    .cart__upsell-title {
      font-size: 29px;
      padding: 33px 0 20px;
    }

    .cart__upsell-slider {
      width: 100%;
    }

    .cart__upsell-slider .slick-slide {
      width: 328px;
      margin: 0 17.5px;
    }

    .cart_upsell-list-items.cart__upsell-slider > .cart__upsell-product {
      width: 220px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) {
      width: 324px;
      translate: 0 2px;
    }

    .cart__upsell-slider.slick-slider .cart__upsell-product-image-wrapper {
      height: 352px;
    }

    .cart__upsell-slider.slick-slider .slick-slide:not(.slick-center) .cart__upsell-product-image-wrapper {
      height: 348px;
    }

    .cart__upsell-product-title {
      font-size: 20px;
      margin-top: 21px;
      padding: 0 14.5px;
      letter-spacing: 0.7px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-title {
      margin-top: 23px;
      padding: 0px 13px;
    }

    .cart__upsell-slider .slick-arrow {
      top: 207px;
    }

    .cart__upsell-slider .prev-arrow {
      left: 40px;
    }
  
    .cart__upsell-slider .next-arrow {
      right: 40px;
      transform: rotate(180deg);
    }

    .cart__upsell-product-review {
      padding: 0 13px;
      margin-top: 11.8px;
      gap: 12.7px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-review {
      padding: 0 11px;
      margin-top: 10.9px;
      gap: 12.6px;
    }

    .cart__upsell-product-review svg {
      width: 67.5px;
    }

    .cart__upsell-product-review span {
      letter-spacing: 0;
    }

    .cart__upsell-atc-form {
      padding: 0 14px 0 13px;
      margin-top: 8.3px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-form {
      padding: 0 13.5px 0 13px;
      margin-top: 9px;
    }

    .cart__upsell-select-wrapper {
      gap: 22px;
    }

    .cart__upsell-product-select {
      height: 22px;
      font-size: 14px;
      padding: 1px 21px 0 8px;
      background-position: 92% 51%; 
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-select {
      padding: 0 22px 1px 7px;
      font-size: 14px;
      letter-spacing: -0.2px;
      background-position: 88% 48%; 
    }

    .cart__upsell-product-atc-wrapper {
      gap: 12px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-atc-wrapper{
      gap: 10px;
      padding: 0;
    }

    .cart__upsell-atc-price {
      height: 34px;
      font-size: 16px;
      letter-spacing: 0; 
      gap: 6px;
      padding-bottom: 2px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-price {
      font-size: 15px;
      height: 35px;
      letter-spacing: 0.2px; 
      padding: 0 0 0 1px;
    }

    .cart__upsell-product-price {
      font-size: 20px;
      letter-spacing: -0.1px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-price {
      font-size: 19px;
      letter-spacing: 0.4px;
    }
  }

  @media (max-width: 767px) {
    .cart__upsel-wrapper {
      padding: 0;
      margin-bottom: 0;
    }
    
    .cart__upsell-title {
      font-size: 21px;
      padding: 18px 0 17px;
      letter-spacing: -0.3px
    }

    .cart__upsell-slider .slick-slide {
      width: 207px;
      margin: 0 10.5px;
    }

    .cart_upsell-list-items.cart__upsell-slider > .cart__upsell-product {
      width: 200px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) {
      width: 200px;
      translate: 0 1px;
    }

    .cart__upsell-slider .slick-arrow {
      width: 40px;
      height: 40px;
      top: 142px;
    }

    .cart__upsell-slider .prev-arrow {
      left: 16px;
    }

    .cart__upsell-slider .next-arrow {
      right: 16px;
      transform: rotate(180deg);
    }

    .cart__upsell-slider.slick-slider .cart__upsell-product-image-wrapper {
      height: 221px;
    }

    .cart__upsell-slider.slick-slider .slick-slide:not(.slick-center) .cart__upsell-product-image-wrapper {
      height: 218px;
    }

    .cart__upsell-product-title {
      font-size: 13px;
      padding: 0 9.4px;
      margin-top: 13px;
      letter-spacing: 0.45px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-title {
      margin-top: 15px;
      padding: 0px 8.5px;
      font-size: 12px;
      letter-spacing: 0.9px;
      line-height: 1.4;
    }

    .cart__upsell-product-review {
      padding: 0 8px;
      margin-top: 4px;
      gap: 8px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-review {
      padding: 0 8px;
      margin-top: 1px;
      gap: 9px;
    }

    .cart__upsell-product-review svg {
      width: 43px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-review svg {
      width: 43px;
    }

    .cart__upsell-product-review span {
      font-size: 8px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-review span {
      font-size: 8px;
    }

    .cart__upsell-atc-form {
      padding: 0 8px;
      margin-top: 1px;
      margin-bottom: 4px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-form {
      padding: 0 8px;
      margin-top: 1px;
      margin-bottom: 5px;
    }

    .cart__upsell-select-wrapper {
      gap: 17px;
    }

    .cart__upsell-product-select {
      font-size: 10px;
      line-height: 1.1;
      height: 15px;
      width: 48%;
      padding: 0 12px 0 6px;
      background-size: 7px;
      background-position-x: 92%;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-select {
      font-size: 10px;
      padding: 0 12px 0 3px;
      background-position: 91% 50%;
    }

    .cart__upsell-product-atc-wrapper {
      gap: 6px;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-atc-wrapper {
      gap: 7px;
    }

    .cart__upsell-product-price {
      font-size: 13px;
      letter-spacing: -0.6px;
      padding-right: 0.7px;
    }
    
    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-product-price {
      font-size: 13px;
      letter-spacing: -0.3px;
    }

    .cart__upsell-atc-price {
      height: 22px;
      font-size: 10px;
      letter-spacing: 0;
      padding-bottom: 0;
    }

    .cart__upsell-slider .slick-slide:not(.slick-center) .cart__upsell-atc-price {
      font-size: 10px;
      height: 21px;
      letter-spacing: -0.1px;
      padding: 0;
    }

    .cart__upsell-alert {
      font-size: 12px;
    }
  }
</style>

<div id="upsell-cart-template" class="wrapper cart__upsell {{ class_hide }}">
  {% if settings.upsell_title != blank %}
    <h3 class="cart__upsell-title upsell-title">{{ settings.upsell_title }}</h3>
  {% endif %}

  <div class="cart_upsell-list-items cart__upsell-slider">
    {% for product in upsell_products %}
      {%- unless product.requires_selling_plan -%}
        <div class="cart__upsell-product" data-product-id="{{ product.id }}">
          <div class="cart__upsell-product-image-wrapper">
            <a class="cart__upsell-product-image-link" href="{{ product.url | default: '#' }}">
              {%-
                render 'responsive-image' with
                image: product.featured_image,
                width: 400,
                height: 400,
                class: 'cart__upsell-product-image',
                lazyload: true,
                alt: product.featured_image.alt
              -%}
            </a>
          </div>

          <div class="cart__upsell-product-info">
            <div class="cart__upsell-product-title-wrapper">
              <h2 class="cart__upsell-product-title">
                <a class="cart__upsell-product-title-link" href="{{ product.url | default: '#' }}">
                  {{ product.title }}
                </a>
              </h2>
            </div>

            <div class="cart__upsell-product-review">
              {%- render 'reviews-stars' with prod: product -%}
            </div>

            <div class="cart__upsell-atc-wrapper">
              <form action="/cart/add" class="cart__upsell-atc-form">
                <div class="cart__upsell-select-wrapper">
                  {%- assign variants_options = product.options_with_values -%}

                  {% if product.variants.size > 1 %}
                    {% assign values = '' %}
                    {% for variant in product.variants %}
                      {% if variant.available %}
                        {% unless values contains variant.option1 %}
                          {% assign values = values | join: ',' %}
                          {% assign values = values | append: ',' | append: variant.option1 %}
                          {% assign values = values | split: ',' %}
                        {% endunless %}
                      {% endif %}
                    {% endfor %}

                    {%- for option in variants_options -%}
                      <select class="cart__upsell-product-select" data-index="{{ forloop.index }}">
                        <option value="" selected disabled>{{ option.name }}</option>

                        {% if forloop.first %}
                          {%- for value in values -%}
                            {% if value == blank %}{% continue %}{% endif %}
                            <option value="{{ value }}">{{ value }}</option>
                          {%- endfor -%}
                        {% endif %}
                      </select>
                    {%- endfor -%}
                  {% endif %}

                  <select
                    id="upsell-all-product-variants"
                    name="items[{{- forloop.index0 -}}][id]"
                    class="cart__upsell-all-product-variants hide"
                    data-productid="{{ product.id }}"
                  >
                    {%- for variant in product.variants -%}
                      {% if variant.available %}
                        <option value="{{ variant.id }}">{{- variant.title -}}</option>
                      {% endif %}
                    {%- endfor -%}
                  </select>

                  <div class="cart__upsell-product-price-wrapper">
                    <div class="cart__upsell-product-price">
                      <span class="cart__upsell-price">{{ product.price | money }}</span>
                    </div>
                  </div>
                </div>

                <input type="hidden" name="items[{{- forloop.index0 -}}][quantity]" value="1">

                <div class="cart__upsell-product-atc-wrapper">
                  <div class="cart__upsell-atc-btn-wrapper">
                    <button
                      id="upsell-atc-{{ product.id }}"
                      class="cart__upsell-atc-price btn"
                      {% if product.variants.size > 1 %} disabled {% endif %}
                      data-variant="{{ product.selected_or_first_available_variant.id }}"
                    >
                      {{ settings.upsell_atc_btn_text }}
                    </button>
                    <span class="upsell-produce-atc___mask"></span>
                  </div>
                </div>
              </form>
            </div>
            <p class="cart__upsell-alert">Please, select a variant to add to cart.</p>
          </div>
        </div>
      {% endunless %}
    {% endfor %}
  </div>
</div>

{% if section.settings.enable_upsell_lazyload %}
  {% comment %} Slick Script {% endcomment %}
  <script type="lazyload_int">
    setTimeout(function () {
      {% if upsell_products.size > 3 %}
        $('.cart_upsell-list-items').slick({
          prevArrow: '<button type="button" class="prev-arrow"></button>',
          nextArrow: '<button type="button" class="next-arrow"></button>',
          infinite: true,
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: true,
          variableWidth: true,
        });
      {% endif %}
    });
    $('.cart__upsel-wrapper').removeClass('hide');
  </script>

  {% comment %} Cart Script {% endcomment %}
  <script type="lazyload_int">
    setTimeout(async function () {
      {%- if cart.items.size > 0 -%}
        await CartAPI.buildCart();
        let url = '/collections/{{- upsell_collection -}}/products.json';
      {% else %}
        let url = '/collections/{{- upsell_collection -}}/products.json';
      {% endif %}
  
      $.getJSON(url, async function (allProducts) {
        window.ALLPRODUCTS = allProducts;
  
        $(document).on('change', '.cart__upsell-product-select', function () {
          try {
            const $input = $(this);
            const selectedValue = $input.val();
            const $lineItem = $input.closest('.cart__upsell-product');
            const index = parseInt($input.attr('data-index') || 1);
            const nextOptionIndex = index + 1;
            const productID = $lineItem.attr('data-product-id');
            const product = window.ALLPRODUCTS.products.find(function (p) {
              return p.id == productID;
            });
  
            if (!product) {
              throw new Error('Product does not exist.');
            }
  
            if (nextOptionIndex > product.options.length) {
              /* Set the variant */
              let variant = product.variants;
  
              $('.cart__upsell-product-select', $lineItem).each(function(i){
                const value = $(this).val();
  
                variant = variant.filter(function(v) {
                  return value == v['option' + (i + 1)];
                });
              });
  
              if (variant.length > 0) {
                variant = variant[0];
  
                $('.cart__upsell-all-product-variants', $lineItem).prop('disabled', false).val(variant.id).trigger('change');
                $('.cart__upsell-atc-price', $lineItem).prop('disabled', false);
              }
            } else {
              $('.cart__upsell-atc-price', $lineItem).attr('disabled', true);
  
              /* Filter the variants based in the selected option */
              let variants = product.variants.filter(function(variant) {
                return variant.available && variant['option' + index] == selectedValue;
              });

              /* Adjust the variants for the next option */
              $input.nextAll('.cart__upsell-product-select').each(function(){
                const options = $(this).find('option').not('[disabled]');
  
                $(options).remove();
              });
  
              const optionsValues = [];
              
              variants.forEach(function(variant){
                if (!optionsValues.includes(variant['option' + nextOptionIndex])) {
                  optionsValues.push(variant['option' + nextOptionIndex]);
                }
              });
  
              const $nextInput = $input.next('.cart__upsell-product-select');
              const $firstOption = $nextInput.find('option').first();
  
              optionsValues.forEach(function(value){
                const newOption = document.createElement('option');
                
                newOption.setAttribute('value', value);
                newOption.innerHTML = value;
                
                $nextInput.append(newOption);
              });
  
              $nextInput.val($firstOption.val());
              $nextInput.removeAttr('disabled');
            }
          } catch (err) {
            console.error(err);
          }
        });
      });
  
      $(document).on('submit', 'form[action*="/cart/add"]', async function (e) {
        const $form = $(this);
        const quantity = $form.find('[name="quantity"]').val();
        const id = $form.find('[name="id"]').val();
  
        e.preventDefault();
  
        await CartAPI.addItem($form.serialize());
        await CartAPI.buildCart();
      });
    }, 1000);
  </script>
{% else %}
  {% comment %} Slick Script {% endcomment %}
  <script>
    window.addEventListener('DOMContentLoaded', function () {
      $('.cart__upsel-wrapper').removeClass('hide');
      {% if upsell_products.size > 3 %}
        $('.cart_upsell-list-items').slick({
          prevArrow: '<button type="button" class="prev-arrow"></button>',
          nextArrow: '<button type="button" class="next-arrow"></button>',
          infinite: true,
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: true,
          variableWidth: true,
        });
      {% endif %}
    });
  </script>

  {% comment %} Cart Script {% endcomment %}
  <script>
    window.addEventListener('DOMContentLoaded', async function () {
      {%- if cart.items.size > 0 -%}
        await CartAPI.buildCart();
        let url = '/collections/{{- upsell_collection -}}/products.json';
      {% else %}
        let url = '/collections/{{- upsell_collection -}}/products.json';
      {% endif %}
  
      $.getJSON(url, async function (allProducts) {
        window.ALLPRODUCTS = allProducts;

        $(document).on('click', '.upsell-produce-atc___mask', function (event) {
          const [ $alert_element ] = $(this).closest('.cart__upsell-product-info').children('.cart__upsell-alert');
          $($alert_element).show();
          setTimeout(() => {
            $($alert_element).hide();
          }, 5000);
        });
  
        $(document).on('change', '.cart__upsell-product-select', function () {
          try {
            const $input = $(this);
            const selectedValue = $input.val();
            const $lineItem = $input.closest('.cart__upsell-product');
            const index = parseInt($input.attr('data-index') || 1);
            const nextOptionIndex = index + 1;
            const productID = $lineItem.attr('data-product-id');
            const product = window.ALLPRODUCTS.products.find(function (p) {
              return p.id == productID;
            });
  
            if (!product) {
              throw new Error('Product does not exist.');
            }
  
            if (nextOptionIndex > product.options.length) {
              /* Set the variant */
              let variant = product.variants;
  
              $('.cart__upsell-product-select', $lineItem).each(function(i){
                const value = $(this).val();
  
                variant = variant.filter(function(v) {
                  return value == v['option' + (i + 1)];
                });
              });
  
              if (variant.length > 0) {
                variant = variant[0];
  
                $('.cart__upsell-all-product-variants', $lineItem).prop('disabled', false).val(variant.id).trigger('change');
                $('.cart__upsell-atc-price', $lineItem).prop('disabled', false);
                $('.upsell-produce-atc___mask', $lineItem).hide();
              }
            } else {
              $('.cart__upsell-atc-price', $lineItem).attr('disabled', true);
              $('.upsell-produce-atc___mask', $lineItem).show();
  
              /* Filter the variants based in the selected option */
              let variants = product.variants.filter(function(variant) {
                return variant.available && variant['option' + index] == selectedValue;
              });
  
              /* Adjust the variants for the next option */
              $input.nextAll('.cart__upsell-product-select').each(function(){
                const options = $(this).find('option').not('[disabled]');
  
                $(options).remove();
              });
  
              const optionsValues = [];
              
              variants.forEach(function(variant){
                if (!optionsValues.includes(variant['option' + nextOptionIndex])) {
                  optionsValues.push(variant['option' + nextOptionIndex]);
                }
              });
  
              const $nextInput = $input.next('.cart__upsell-product-select');
              const $firstOption = $nextInput.find('option').first();
  
              optionsValues.forEach(function(value){
                const newOption = document.createElement('option');
                
                newOption.setAttribute('value', value);
                newOption.innerHTML = value;
                
                $nextInput.append(newOption);
                $nextInput.removeAttr('disabled');
              });
  
              $nextInput.val($firstOption.val());
            }
          } catch (err) {
            console.error(err);
          }
        });
      });
  
      $(document).on('submit', 'form[action*="/cart/add"]', async function (e) {
        const $form = $(this);
        const quantity = $form.find('[name="quantity"]').val();
        const id = $form.find('[name="id"]').val();
  
        e.preventDefault();
  
        await CartAPI.addItem($form.serialize());
        await CartAPI.buildCart();
      });
    });
  </script>
{% endif %}
